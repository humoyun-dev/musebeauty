# Mahalliy dev: backend manzili (Vite dev serveri shu yerga so'rov yuboradi).
# Production'da BO'SH qoldiring yoki olib tashlang — serverdagi nginx (Docker
# tashqarisida) bir xil origin'dan /api/admin ni backendga proxy qiladi
# (dataProvider default "/api/admin").
VITE_API_URL=http://localhost:8000/api/admin
